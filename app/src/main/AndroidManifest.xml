<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 通知与闹钟权限 / Notification and Exact Alarm Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 存储访问权限（支持 SAF 及本地快速访问） / Storage permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:name=".OBpartnerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.OBpartner">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OBpartner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 闹钟广播接收器 / Exact Alarm Broadcast Receiver -->
        <receiver
            android:name=".notification.AlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.obpartner.app.ACTION_TRIGGER_EVENT_ALARM" />
            </intent-filter>
        </receiver>

        <!-- 桌面日历交互显式广播接收器 (100% 防拦截，毫秒级响应翻页与周月切换) -->
        <receiver
            android:name=".widget.CalendarActionReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.obpartner.app.ACTION_CALENDAR_NAV_PREV" />
                <action android:name="com.obpartner.app.ACTION_CALENDAR_NAV_NEXT" />
                <action android:name="com.obpartner.app.ACTION_CALENDAR_RESET_TODAY" />
                <action android:name="com.obpartner.app.ACTION_CALENDAR_SET_VIEW" />
            </intent-filter>
        </receiver>

        <!-- 桌面日历组件 3×2 (横版主推) -->
        <receiver
            android:name=".widget.CalendarWidget3x2Receiver"
            android:enabled="true"
            android:exported="true"
            android:label="@string/widget_calendar_3x2_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/calendar_widget_3x2_info" />
        </receiver>

        <!-- 桌面日历组件 2×3 (竖版主推) -->
        <receiver
            android:name=".widget.CalendarWidget2x3Receiver"
            android:enabled="true"
            android:exported="true"
            android:label="@string/widget_calendar_2x3_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/calendar_widget_2x3_info" />
        </receiver>

        <!-- 桌面日历组件 2×2 (方形精简) -->
        <receiver
            android:name=".widget.CalendarWidget2x2Receiver"
            android:enabled="true"
            android:exported="true"
            android:label="@string/widget_calendar_2x2_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/calendar_widget_2x2_info" />
        </receiver>

        <!-- 桌面待办组件 3×2 (横版主推) -->
        <receiver
            android:name=".widget.TodayTasksWidget3x2Receiver"
            android:enabled="true"
            android:exported="true"
            android:label="@string/widget_today_tasks_3x2_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_tasks_widget_3x2_info" />
        </receiver>

        <!-- 桌面待办组件 2×2 (方形精简看板) -->
        <receiver
            android:name=".widget.TodayTasksWidget2x2Receiver"
            android:enabled="true"
            android:exported="true"
            android:label="@string/widget_today_tasks_2x2_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_tasks_widget_2x2_info" />
        </receiver>


    </application>

</manifest>
